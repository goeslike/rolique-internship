const router = require('express').Router();
// const { authController } = require('../controllers');
//
// router.post('/login',);
// router.post('/logout', );
// router.post('/refresh', );

module.exports = router;
